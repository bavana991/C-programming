#include <stdio.h>

int main() {

    int n, i;

    float sum = 0.0, mean;

    int arr[100]; 

    scanf("%d", &n);

    for (i = 0; i < n; i++) {

        scanf("%d", &arr[i]);

        sum += arr[i];

    }

    mean = sum / n;

    printf("Array Mean Value is %.2f\n", mean);

    return 0;

}
